# Debug Code

audit.debug_code.name=Debug code
audit.debug_code.description=\
  Search for apparent debug code such as calls to \
  <CODE>System.out.println()</CODE> or \
  <CODE>Throwable.printStackTrace()</CODE>

# Early declaration check

audit.early_declaration.name=Early declaration check
audit.early_declaration.description=Check if variables are declared too early <br> Example: <p>int i = 0; <br>System.err.print("aaa"); <br>int x = i; </p> Variable i should be declared as closely to first use as possible, after line <blockquote><CODE> System.err.print("aaa");</CODE></blockquote>

# Duplicate strings check
audit.duplicate_strings.name=Duplicate strings check
audit.duplicate_strings.description=Check if there are 2 or more sam strings in class


